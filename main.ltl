// ltl-phred/main.ltl
//
// Phred-quality converter for Sanger / Illumina encodings.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-phred v0.1.0")
    io::println("Phred-quality converter for Sanger / Illumina encodings.")
    0
}
