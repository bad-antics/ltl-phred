// examples/demo.ltl — runnable demo for ltl_phred

import std::io
use ltl_phred as lib

fn main() {
    io::println("ltl_phred demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}